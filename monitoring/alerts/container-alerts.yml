groups:
  - name: container-alerts

    rules:

      - alert: ContainerDown
        expr: up == 0
        for: 1m

        labels:
          severity: critical

        annotations:
          summary: "Container or exporter is down"
          description: "Prometheus target has not responded for more than 1 minute."
